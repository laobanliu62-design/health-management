apiVersion: v1
kind: Service
metadata:
  name: {{ include "health-mgmt.fullname" . }}
  labels:
    {{- include "health-mgmt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "health-mgmt.selectorLabels" . | nindent 4 }}
